{
  "name": "coffee",
  "private": true,
  "description": "Internet shop for coffee and coffee machines",
  "scripts": {
    "prepare": "husky",
    "dev": "docker compose -f infra/docker-compose.yml up --build",
    "dev:down": "docker compose -f infra/docker-compose.yml down",
    "dev:reset": "docker compose -f infra/docker-compose.yml down -v && docker compose -f infra/docker-compose.yml up --build"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7"
  }
}
